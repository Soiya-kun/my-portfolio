{
  "name": "my-portfolio",
  "private": true,
  "scripts": {
    "check": "npm run build && node scripts/check-gantt.mjs && tsc --noEmit && node scripts/check-private.mjs",
    "synth": "npm run build && cdk synth",
    "diff": "npm run build && cdk diff",
    "deploy": "npm run check && cdk deploy",
    "private:init": "node scripts/private.mjs",
    "build": "node scripts/build-site.mjs",
    "watch:content": "node scripts/watch-site.mjs"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "aws-cdk": "^2.1140.0",
    "marked": "^18.0.11",
    "ts-node": "^10.9.2",
    "turndown": "^7.2.4",
    "typescript": "~5.9.3"
  }
}
